package Course1.day1;

public class StringBufferAndBuilder {
        public static void main(String[] args) {
            /*
             * String is immutable
             * Stringbuffer and stringbuilder are mutable
             * Strigbuilder is faster than stringbuffer because, buffer is trying to be synchronized,
             * builder is not trying to be synchronized.
             * */
        }
    }

